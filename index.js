//função que chama um alert avisando ao usuário que a mensagem foi enviada
function Message() {
  alert("Mensagem enviada com sucesso! Irei retornar em breve :)");
}

//função que limpa todos os inputs e textareas, substituindo por vazio ""
function Clean() {
  $("input").val("");
  $("textarea").val("");
}
